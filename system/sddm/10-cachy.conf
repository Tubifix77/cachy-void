# Cachy-Void — select the branded SDDM greeter (installed by deploy.sh --with-branding).
# Removing this file (or `deploy.sh --uninstall`) reverts SDDM to its default theme.
[Theme]
Current=void-tactical
