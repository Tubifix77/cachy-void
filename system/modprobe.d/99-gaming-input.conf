# Cachy-Void legacy HID polling — architecture.md §3.3
#
# Installed to /etc/modprobe.d/99-gaming-input.conf by deploy.sh.
# Lifts legacy HID devices to 1000 Hz. Modern gaming mice negotiate their
# native rate regardless; this only affects devices that don't.
options usbhid mousepoll=1
